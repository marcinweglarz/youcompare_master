<h1>Listing package_of_products</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @package_of_products.each do |package_of_product| %>
  <tr>
    <td><%= link_to 'Show', package_of_product %></td>
    <td><%= link_to 'Edit', edit_package_of_product_path(package_of_product) %></td>
    <td><%= link_to 'Destroy', package_of_product, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Package of product', new_package_of_product_path %>
